# regex_validation.py
A Python project that uses regular expressions to validate email addresses, Indian mobile numbers, and passwords. Demonstrates clean input validation, reusable functions, and edge case handling using the built-in re module.
